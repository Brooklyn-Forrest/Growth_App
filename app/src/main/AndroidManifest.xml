<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.growth">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".data_page"></activity>
        <activity android:name=".create_goal" />
        <activity android:name=".goal_progress_overview" />
        <activity android:name=".main_view_account" />
        <activity android:name=".introduction">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".setup_pass_settings" />
        <activity android:name=".MainActivity" />
        <activity android:name=".settings_page" />
    </application>

</manifest>